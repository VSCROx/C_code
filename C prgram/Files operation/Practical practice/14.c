// Using fopen(), fgetc(), fputc() and fclose() functions write a program that reads characters from the keyboard and writes them to a disk file until the user type a dollar sign. The filename is specified from the command line.

#include<stdio.h>

int main(){
    FILE* fptr = NULL;
    fptr = fopen("result.txt","w");

    int ch;
    while(ch=fputc)
    return 0;
}