#include<stdio.h>
#include<stdbool.h>
// #define MAX_LIMIT 20
int main(){
    // char str[10];
    // gets(str);
    // printf("%s", str);

    // // Method 2
    // char str[MAX_LIMIT];
    // fgets(str, MAX_LIMIT, stdin);
    // printf("%s", str);

    // Printf execute from Right to Left
    // int x = 15;
    // printf("%d %d %d",x=15,x=20,x<30);
    
    return 0;
}